- datetime:
    text_size: l
    format:
      timeStyle: short
      dateStyle: short
- resources:
    cpu: true
    memory: true
    disk: /
    cputemp: true
    uptime: true
    units: imperial
    refresh: 3000 # optional, in ms
    diskUnits: bytes # optional, bytes (default) or bbytes. Only applies to disk
    network: true

- openmeteo:
    label: Washington
    units: matric 
    cache: 10

- grafana:
