services:
  adguard_home:
    container_name: adguard_home
    image: adguard/adguardhome:latest
    restart: unless-stopped
    networks:
      - adguard_net
    volumes:
      - config:/opt/adguardhome/conf # app config
      - work:/opt/adguardhome/work # app working dir
    ports:
      - 53:53/udp
      - 53:53/tcp

networks:
  adguard_net:
    driver: bridge
    name: adguard_net

volumes:
  config:
    driver: local
  work:
    driver: local

